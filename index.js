console.log("Nothing is worth more than laughter.");
var bestSong = Math.random() > 0.5
    ? "resect1" : "Respect";
var rocker;
rocker = 'faizan';
console.log(rocker);
rocker = 1;
console.log(rocker);
