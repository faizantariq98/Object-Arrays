console.log("Nothing is worth more than laughter.");
const bestSong = Math.random() > 0.5 
  ? "resect1"   : "Respect";


  let rocker;

  rocker='faizan';
  console.log(rocker);

  rocker=1;
  console.log(rocker);